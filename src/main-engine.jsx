import React from 'react';
import ReactDOM from 'react-dom/client';
import AuditEngine from './components/VERNEN_Autonomous_Audit_Engine';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AuditEngine />
  </React.StrictMode>
);
