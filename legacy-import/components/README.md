# components/ (recovered)

JSX components recovered from `ProtonDrive/Desktop/MASTER_VERNEN/05_Components/` on 2026-05-05. None are present in the current `src/components/` tree.

| File | Notes |
|---|---|
| `VERNEN_Simulator_v7.jsx` | Civil suit strategy simulator — 16 modules, ~1,400 lines |
| `VERNEN_Simulator_v6.jsx` | Prior version — ~1,242 lines |
| `vernen-simulator.jsx` | Standalone simulator variant |
| `vernen-simulator-variant1.jsx` | Second standalone variant (was `vernen-simulator (1).jsx`) |

The `VERNEN_Audit_Engine`, `VERNEN_Autonomous_Audit_Engine`, and `VERNEN_Case_Manager` components in MASTER_VERNEN are mirrored in current `src/components/` so are not re-imported.
